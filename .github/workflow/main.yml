name: Linters

on:
pull_request:
    branches: [master]
    paths:
      - "**.py"

jobs:
  flake8:
    name: flake8
    runs-on: ubuntu-latest
    steps:
      - name: Checkout action
        uses: actions/checkout@v2
      - name: Django Coverage action
        uses: DyedykStanislav/own_linter@v1